name: package_validation
description: Validate totalis_types package contents
version: 1.0.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  totalis_types: ^1.0.99

dev_dependencies:
  flutter_test:
    sdk: flutter